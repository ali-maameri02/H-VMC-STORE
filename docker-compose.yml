version: '3.9'

services:
  backend:
    build: /root/H-VMC-STORE/Backend
    image: hvmc-backend
    container_name: hvmc_backend
    env_file: .env.prod
    volumes:
      - static_volume:/app/static
      - media_volume:/app/media
    networks:
      - hvmc-network
    restart: unless-stopped

  frontend:
    build: /root/H-VMC-STORE/hvmc-store
    image: hvmc-frontend
    container_name: hvmc_frontend
    ports:
    - "5173:5173"  # Add this for Vite HMR in development
    networks:
      - hvmc-network
    restart: unless-stopped

  db:
    image: postgres:16
    container_name: hvmc_db
    volumes:
      - pg_data:/var/lib/postgresql/data/
    env_file: .env.prod
    networks:
      - hvmc-network
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    container_name: hvmc_nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
     - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
     - ./nginx/conf.d:/etc/nginx/conf.d:ro
     - static_volume:/app/static
     - media_volume:/app/media
     - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - backend
      - frontend
    networks:
      - hvmc-network
    restart: unless-stopped

volumes:
  pg_data:
  static_volume:
  media_volume:

networks:
  hvmc-network:
    driver: bridge